server:
  servlet:
    context-path: /api/v2/poli
  port: 9080
spring:
  application:
    name: bookings
  profiles:
    active: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9010/eureka/

